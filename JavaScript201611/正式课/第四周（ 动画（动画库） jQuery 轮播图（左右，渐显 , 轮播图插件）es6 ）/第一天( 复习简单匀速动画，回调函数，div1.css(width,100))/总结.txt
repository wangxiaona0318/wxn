1 动画： 匀速动画 减速动画
2 匀速动画 : 规定一个不变的速度 5
           也可以计算出一个速度  距离/时间
           利用等比公式计算出当前的位置
           function linear(t,b,c,d){
                return b + t/d*c;
           }
           t是花费的时间
           b是开始的位置
           c是改变的距离   target(终点) - begin（起点）
           d是时间间隔
3 左右方向动画 ： 定时器问题(在move函数开始执行的时候先清定时器，清的是上一次还没有到达终点的定时器)
4 回调函数（node模块的回调函数应用比较多）,当运动到达终点之后我还要继续操作这个盒子，比如变颜色等。其实就是回调函数的引用。
5 div1.setCss(width,100)  ==?   setCss(div1,width,100)
6 利用原型链结构 Element.prototype.setCss = function (attr,val){
                    this.style[attr] = val;
              }
7 选项卡封装，如何把事件也当作参数的？默认事件？默认选中？ ||
8 是否可以把一个类直接添加到Object的原型上？？？？
9 这周实现把一个轮播图使用构造函数的方式写出来。。。


